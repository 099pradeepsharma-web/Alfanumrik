// This file is intentionally left blank.
// The Firebase configuration was using placeholder credentials, which caused the app to crash.
// The application has been switched to use a mock API (`services/mockApi.ts`)
// for demonstration and preview purposes.
// To re-enable Firebase, provide valid credentials in a `firebaseConfig` object,
// initialize the app, and update `services/authService.ts` to use the Firebase SDK.
